with_entries(if (.value | type) == "object" then .value = (.value | .a = .address + " " + .city + " " + .state + " " + .zip | del(.address) | del(.city) | del(.state) | del(.zip) | del(.x) ) else . end)